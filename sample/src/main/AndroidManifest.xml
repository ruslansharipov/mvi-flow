<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.sharipov.mvi.flow.app">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mviflow">

        <activity
            android:name=".compose.ComposeActivityView"
            android:exported="true"
            android:label="@string/title_activity_compose_view"
            android:theme="@style/Theme.Mviflow.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".simple.SimpleActivity"
            android:exported="true">

        </activity>
    </application>

</manifest>